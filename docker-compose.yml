version: '3.8'

services:
  api:
    build: .
    container_name: koscheck_backend
    restart: always
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./secrets:/app/secrets:ro
